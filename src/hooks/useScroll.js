// import { useState, useEffect } from "react";
// import { useListener } from "./useListener";

// export const useScroll = (ref = {current: window}) => {
//   const [scrolledFromTop, setScrolledFromTop] = useState(0);

//   const onScroll = (e) => {
//     setScrolledFromTop(ref.current.scrollY)
//   };

//   useListener("scroll", onScroll, ref);

//   return { scrolledFromTop };
// };
